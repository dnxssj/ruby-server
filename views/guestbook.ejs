<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guestbook</title>
    <link rel="icon" type="image/x-icon" href="/images/ruby.ico">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <a href="/"><img src="/images/logo.png" alt="Ruby Server Logo"></a>
    </header>
    <nav>
        <a href="/">Inicio</a>
        <a href="/jugadores">Top All Time</a>
        <a href="/clanes">Clanes</a>
        <a href="/actual">Top Actual</a>
        <a href="/hemeroteca">Hemeroteca</a>
        <a href="/guestbook" class="actual">Guestbook</a>
    </nav>
    <div class="container">
        <h1>Guestbook</h1>
        <form action="/guestbook" method="POST" class="guestbook-form">
            <textarea name="message" placeholder="Escribe tu comentario aquí..." required></textarea>
            <input type="text" name="name" placeholder="Tu nombre" required>
            <button type="submit">Enviar</button>
        </form>
        <div class="messages">
            <h2>Comentarios previos</h2>
            <% messages.forEach(message => { %>
                <div class="comment">
                    <p><strong><%= message.name || "Anonymous" %>:</strong> <%= message.text %></p>
                    <% if (cookies && message.userId === cookies.userId) { %>
                        <form action="/guestbook/delete/<%= message.id %>" method="POST" style="display:inline;">
                            <button type="submit">Eliminar</button>
                        </form>
                        <form action="/guestbook/edit/<%= message.id %>" method="POST" style="display:inline;">
                            <input type="text" name="message" placeholder="Editar comentario" required>
                            <button type="submit">Actualizar</button>
                        </form>
                    <% } %>
                </div>
            <% }) %>
        </div>        
    </div>
    <footer>
        <p>&copy; 2024 Ruby Server. Todos los derechos reservados.</p>
    </footer>
</body>
</html>